import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.avolutions.timetracking',
  appName: 'TimeTracking',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
